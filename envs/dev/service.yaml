apiVersion: v1
kind: Service
metadata:
  name: podinfo
  namespace: dev
spec:
  selector:
    app: podinfo
  ports:
  - name: http
    port: 80
    targetPort: 9898
    protocol: TCP
  type: ClusterIP
